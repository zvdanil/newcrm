{
  "name": "iris-crm",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "backend",
    "frontend",
    "db"
  ],
  "scripts": {
    "predev": "docker compose up -d postgres && docker compose exec postgres sh -c 'until pg_isready -U iris -d iris_db; do sleep 1; done'",
    "dev": "concurrently -n \"backend,frontend\" -c \"blue,green\" \"npm run dev --workspace=backend\" \"npm run dev --workspace=frontend\"",
    "build": "npm run build --workspace=frontend && npm run build --workspace=backend",
    "start": "node db/migrate.js && node backend/dist/server.js",
    "db:migrate": "npm run migrate --workspace=db",
    "db:seed": "npm run seed --workspace=db",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down",
    "docker:reset": "docker compose down -v && docker compose up -d"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
